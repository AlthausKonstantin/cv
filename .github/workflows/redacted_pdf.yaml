name: Compile Public Version
on:
  push:
    branches:
      - main
      - feat/*
jobs:
  make_redacted_pdf:
    uses: ./.github/workflows/compile_pdf.yml
    with:
      artifact-name: "Redacted CV"
