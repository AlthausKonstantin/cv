name: Compile Redacted CV
on:
  push:
    branches-ignore:
      - main
permissions:
  contents: write
jobs:
  compile_redacted_cv:
    name: Compile Redacted CV
    uses: ./.github/workflows/compile_pdf.yaml
    with:
      artifact-name: RedactedCV
    secrets: inherit